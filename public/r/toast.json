{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "toast",
  "title": "Toast",
  "description": "Toast card plus a Toaster with a window.toast() runtime API.",
  "registryDependencies": [
    "@squishy/styles",
    "@squishy/icon"
  ],
  "files": [
    {
      "path": "src/components/Toast.astro",
      "content": "---\n/**\n * One toast card. For runtime toasts use <Toaster /> in the layout and call\n * window.toast({ title, message, color, icon }) or put data-toast='{...}' on a button.\n */\nimport Icon from './Icon.astro';\nimport { colorStyle, type ColorName } from './palette';\n\ninterface Props {\n  title: string;\n  message?: string;\n  color?: ColorName;\n  icon?: string;\n  class?: string;\n}\n\nconst { title, message, color = 'zinc', icon = 'info', class: className } = Astro.props;\n---\n\n<div class:list={['toast', className]} role=\"status\" style={colorStyle(color)}>\n  <span class=\"toast-icon\"><Icon name={icon} /></span>\n  <div class=\"toast-body\">\n    <div class=\"toast-title\">{title}</div>\n    {message && <div class=\"toast-message\">{message}</div>}\n  </div>\n  <button type=\"button\" class=\"toast-close\" aria-label=\"Dismiss\" data-toast-close>\n    <Icon name=\"close\" />\n  </button>\n</div>\n",
      "type": "registry:file",
      "target": "~/src/components/Toast.astro"
    },
    {
      "path": "src/components/Toaster.astro",
      "content": "---\n/**\n * Toast container + runtime API. Put it once in the layout.\n *\n *   window.toast({ title: 'Saved', message: 'Slot 1', color: 'green', icon: 'check-circle' })\n *   <Button data-toast='{\"title\":\"Saved\",\"color\":\"green\",\"icon\":\"check-circle\"}'>Save</Button>\n *\n * Icons are pre-rendered here, because astro-icon runs at build time.\n * Add a name to `icons` to make it available at runtime.\n */\nimport Icon from './Icon.astro';\nimport Toast from './Toast.astro';\nimport { colorNames, colorStyle } from './palette';\n\nconst icons = ['info', 'check-circle', 'warning', 'star', 'save', 'delete', 'favorite', 'shopping-cart'];\nconst styles = Object.fromEntries(colorNames.map((c) => [c, colorStyle(c)]));\n---\n\n<div class=\"toaster\" data-toaster data-colors={JSON.stringify(styles)} aria-live=\"polite\"></div>\n\n<template data-toast-template><Toast title=\"\" /></template>\n{icons.map((n) => <template data-toast-icon={n}><Icon name={n} /></template>)}\n\n<script>\n  type ToastOptions = {\n    title: string;\n    message?: string;\n    color?: string;\n    icon?: string;\n    /** ms; 0 = stays until closed */\n    duration?: number;\n  };\n\n  const toaster = document.querySelector<HTMLElement>('[data-toaster]');\n  const colors: Record<string, string> = toaster ? JSON.parse(toaster.dataset.colors || '{}') : {};\n\n  function dismiss(el: HTMLElement) {\n    if (el.classList.contains('is-leaving')) return;\n    el.classList.add('is-leaving');\n    el.addEventListener('animationend', () => el.remove(), { once: true });\n  }\n\n  function toast(opts: ToastOptions) {\n    if (!toaster) return;\n    const tpl = document.querySelector<HTMLTemplateElement>('[data-toast-template]')!;\n    const el = (tpl.content.firstElementChild as HTMLElement).cloneNode(true) as HTMLElement;\n    el.querySelector('.toast-title')!.textContent = opts.title;\n    const body = el.querySelector('.toast-body')!;\n    body.querySelector('.toast-message')?.remove();\n    if (opts.message) {\n      const m = document.createElement('div');\n      m.className = 'toast-message';\n      m.textContent = opts.message;\n      body.appendChild(m);\n    }\n    el.setAttribute('style', colors[opts.color ?? 'zinc'] ?? colors.zinc ?? '');\n    const iconTpl = document.querySelector<HTMLTemplateElement>(`[data-toast-icon=\"${opts.icon ?? 'info'}\"]`);\n    if (iconTpl) el.querySelector('.toast-icon')!.replaceChildren(iconTpl.content.cloneNode(true));\n    toaster.appendChild(el);\n    const duration = opts.duration ?? 4000;\n    if (duration > 0) setTimeout(() => dismiss(el), duration);\n    return el;\n  }\n\n  (window as any).toast = toast;\n\n  document.addEventListener('click', (e) => {\n    const t = e.target as HTMLElement;\n    const close = t.closest<HTMLElement>('[data-toast-close]');\n    if (close) return dismiss(close.closest<HTMLElement>('.toast')!);\n    const trigger = t.closest<HTMLElement>('[data-toast]');\n    if (trigger) {\n      e.preventDefault();\n      try {\n        toast(JSON.parse(trigger.dataset.toast!));\n      } catch {\n        toast({ title: trigger.dataset.toast! });\n      }\n    }\n  });\n</script>\n",
      "type": "registry:file",
      "target": "~/src/components/Toaster.astro"
    }
  ],
  "categories": [
    "game-ui"
  ],
  "type": "registry:item"
}